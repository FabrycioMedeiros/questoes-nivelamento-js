// Escreva uma função chamada concatenar que recebe duas strings como parâmetros e retorna a concatenação delas.

function concatenar(string1, string2) {
    return string1 + string2;
}

// Uso da função
const string1 = "Fabrycio ";
const string2 = "Medeiros";
const resultado = concatenar(string1, string2);
console.log(resultado); 