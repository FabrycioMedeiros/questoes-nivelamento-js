//Crie uma função chamada quadrado que aceita um número como parâmetro e retorna o quadrado desse número.

function quadrado(numero) {
    return numero * numero;
}

// Uso da função
const numero = 8;
const resultado = quadrado(numero);
console.log(`O quadrado de ${numero} é ${resultado}`);